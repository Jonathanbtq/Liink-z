{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/style.css') }}">
{% endblock %}

<nav class="navbar navbar-expand-lg navbar-light nav_link">
	<div class="w-75">
		<a class="nav-link" href="{{ path('main') }}">Link'Web</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
	</div>

	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar_nav_rgt">
			{% if app.user %}
				<li class="nav_item_rgt">
					<a class="nav_a" href="{{ path('logout') }}">Deconnection</a>
				</li>
			{% else %}
				<li class="nav_item_rgt">
					<a class="nav_a" href="{{ path('app_register') }}">Account creation</a>
				</li>
				<li class="nav_item_rgt">
					<a class="nav_a" href="{{ path('login') }}">Login</a>
				</li>
			{% endif %}
		</ul>
	</div>
	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav">
			<li class="nav_a">
				{% if app.user %}
					<a class="nav_a" href="{{ path('show', {id: app.user.id}) }}">Profile</a>
				{% else %}
					<a class="nav-a" href="{{ path('login') }}">Profile</a>
				{% endif %}
			</li>
		</ul>
	</div>
</nav>
